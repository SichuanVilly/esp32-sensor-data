idf_component_register(
    SRCS "dht.c"        # List of source files for this component (e.g., temperature.c)
    INCLUDE_DIRS "."            # Include the current directory as an include path for headers
)